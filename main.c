#include "mandelbrot.h"
#include "implementacoes.h"
#include "imagem.h"
 
/*
 * Ponto de entrada do programa.
 *
 * Vai ser responsavel por:
 *   - ler e validar os argumentos (largura, altura, max_iteracoes, num_threads)
 *   - rodar as quatro implementacoes
 *   - cronometrar cada uma
 *   - gravar os arquivos .pgm e o times.txt
 *
 * (esqueleto por enquanto - so garante que tudo compila)
 */
int main(int argc, char **argv) {
    (void)argc;   /* ainda nao usados - evita warning */
    (void)argv;
    return 0;
}
 
